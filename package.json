{
  "name": "medyia",
  "private": true,
  "version": "1.0.0",
  "description": "Plateforme clinique — frontend + backend",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "dev:api": "npm --prefix backend start",
    "dev:web": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
